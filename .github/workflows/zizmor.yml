name: GitHub Actions Security Analysis with zizmor 🌈

on:
  pull_request:
    paths: &paths
      - '.github/**.yml'
      - '.github/**.yaml'
  push:
    branches: ['master']
    paths: *paths

permissions:
  actions: read  # for zizmor workflow API
  contents: read  # for reading workflow files

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  zizmor:
    uses: yiisoft/actions/.github/workflows/zizmor.yml@master
